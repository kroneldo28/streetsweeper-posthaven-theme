<section class="error-404">
  <div class="error-icon">
    <svg xmlns="http://www.w3.org/2000/svg"
         width="150" height="150"
         viewBox="0 0 2048 2048"
         role="img" aria-label="Kouakep icon bounce">
      <style>
        @keyframes bounce {
          0%, 100% { transform: translateY(0); }
          40%      { transform: translateY(-140px); }
          60%      { transform: translateY(0); }
          75%      { transform: translateY(-60px); }
          88%      { transform: translateY(0); }
          95%      { transform: translateY(-18px); }
        }

        .bounce {
          transform-origin: 1024px 1024px;
          animation: bounce 1.25s ease-in-out infinite;
          will-change: transform;
        }

        @media (prefers-reduced-motion: reduce) {
          .bounce { animation: none; }
        }
      </style>

      <rect width="2048" height="2048" fill="transparent"/>

      <g class="bounce">
        <image x="0" y="0" width="2048" height="2048"
               href="https://ronelkouakep.com/images/icon-k.png"
               preserveAspectRatio="xMidYMid meet"/>
      </g>
    </svg>
  </div>

  <h1 class="error-code">404</h1>
  <h2 class="error-title" data-i18n="error.title">Page introuvable</h2>
  <p class="error-message" data-i18n="error.message">Oups ! La page que tu cherches n'existe pas ou a été déplacée.</p>

  <div class="error-actions">
    <a href="/" class="btn-primary" data-i18n="error.backHome">Retour à l'accueil</a>
    <a href="/archive" class="btn-secondary" data-i18n="error.browseArchives">Parcourir les archives</a>
  </div>
</section>

<style>
.error-404 {
  text-align: center;
  padding: 3rem 1.5rem;
  min-height: 60vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.error-icon {
  width: 150px;
  height: 150px;
  margin-bottom: 2rem;
}

.error-code {
  font-size: 6rem;
  font-weight: 700;
  color: #F9B513;
  margin: 0;
  line-height: 1;
  text-shadow: 0 4px 20px rgba(249, 181, 19, 0.3);
}

.error-title {
  font-size: 1.5rem;
  color: #fff;
  margin: 1rem 0 0.5rem;
}

.error-message {
  color: rgba(255, 255, 255, 0.6);
  font-size: 1rem;
  max-width: 400px;
  margin: 0 auto 2rem;
}

.error-actions {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
  justify-content: center;
}

.error-actions .btn-primary {
  background-color: #F9B513;
  color: #021923;
  padding: 0.75rem 1.5rem;
  border-radius: 0.5rem;
  font-weight: 600;
  transition: all 0.3s ease;
}

.error-actions .btn-primary:hover {
  background-color: #F1890C;
  transform: translateY(-2px);
}

.error-actions .btn-secondary {
  background-color: transparent;
  color: #F9B513;
  padding: 0.75rem 1.5rem;
  border: 2px solid #F9B513;
  border-radius: 0.5rem;
  font-weight: 600;
  transition: all 0.3s ease;
}

.error-actions .btn-secondary:hover {
  background-color: rgba(249, 181, 19, 0.1);
  transform: translateY(-2px);
}

@media (max-width: 480px) {
  .error-code {
    font-size: 4rem;
  }

  .error-icon {
    width: 120px;
    height: 120px;
  }

  .error-actions {
    flex-direction: column;
    width: 100%;
  }

  .error-actions a {
    width: 100%;
    text-align: center;
  }
}
</style>
